# GMT_Collection_Builder
Builds curated .gmt gene-set files for GSEA/ssGSEA from MSigDB, using editable whitelist/blacklist text files and a review CSV instead of hardcoded gene lists in R. Two-stage filtering (collection-level + biological keyword), manual pathway add/remove support, joint and per-pathway GMT output. R/msigdbr based.
